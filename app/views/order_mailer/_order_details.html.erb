<table>
  <thead>
    <tr>
      <th><%= t ".number_order" %></th>
      <th><%= t ".category" %></th>
      <th><%= t ".name" %></th>
      <th><%= t ".quantity" %></th>
      <th><%= t ".price" %></th>
    </tr>
  </thead>
  <tbody>
    <% order_details.each_with_index do |item, index| %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= item.product.category.name %></td>
        <td><%= item.product.name %></td>
        <td><%= item.quantity %></td>
        <td>
          <strong><%= item.total_price %><%= t "currency" %></strong></td>
      </tr>
    <% end %>
  </tbody>
</table>
